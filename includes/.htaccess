# Carpeta interna: no accesible por HTTP.
# Si Apache 2.4: Require all denied · Apache 2.2: Deny from all.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
